<UserControl x:Class="Dll.Injector.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:Caliburn="http://www.caliburnproject.org"
             xmlns:ViewModels="clr-namespace:Dll.Injector.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance ViewModels:MainViewModel}">
    <ComboBox ItemsSource="{Binding Processes, Mode=OneTime}"
              SelectedIndex="{Binding SelectedProcess}"
              VerticalAlignment="Top">
        <ComboBox.ItemTemplate>
            <DataTemplate>
                <TextBlock FontFamily="Consolas">
                    <Run Text="{Binding Id, Mode=OneTime, StringFormat={}{0:X8}}"/> - <Run Text="{Binding ProcessName, Mode=OneTime}"/>
                </TextBlock>
            </DataTemplate>
        </ComboBox.ItemTemplate>
    </ComboBox>
</UserControl>